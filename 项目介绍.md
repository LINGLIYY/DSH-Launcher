# DSH Launcher（启动器）· 项目介绍

> 当前版本：**v1.0.2（2026-08-20）**，整体项目第一版完成。

## 一、这是什么

DSH Launcher 是一个面向 **DeepSeek Harness（DSH）** 的 Windows 轻量桌面启动器。

它不重新实现 DSH 本体，而是把已经用官方命令安装好的 `dsh` 命令行工具包装成一个更顺手的使用入口：

- 双击启动后，可在控制台手动拉起 DSH 的本地 Web 界面（也可配置为启动时自动拉起、就绪后自动开浏览器）；
- 提供系统托盘，关闭窗口后最小化到托盘；
- 内置日志查看和会话管理；
- 用「浅蓝 + 白」的界面 + 可调看板娘背景，替代单调的黑窗口体验。

一句话概括：**DSH 本体负责“能跑”，这个启动器负责“好用、不崩、轻量”。**

## 二、为什么要做这个项目

早期版本经历过三种形态：

1. Electron 桌面壳：功能齐全但打包重、启动慢、安装包/自解压在 Windows 上反复出问题。
2. Python 启动脚本 + pystray 托盘：轻量但依赖 Python 环境，托盘在 Windows 上不稳定，会话解析和界面都依赖 Python 运行时。
3. 现在的 **Tauri 2（Rust + WebView2）**：既保持轻量，又拥有可靠的系统托盘和原生 WebView 界面。

最终目标：

- 启动器本身不依赖 Python，也不打包 Node/Electron，发布产物只是一个几 MB 的单文件 exe；
- 启动 DSH 的过程完全异步，界面不会被卡死；
- 托盘、窗口、会话管理都稳定可用；
- UI 以豆包生成的设计稿为底板，延续浅蓝白风格和看板娘。

## 三、技术栈

- **桌面壳**：Tauri 2（Rust + 系统 WebView2）
- **后端**：Rust
  - 进程管理：异步启动/停止 `dsh web`
  - 会话解析：`zstd` + `serde_json` 原生读取 DSH 会话
  - 系统托盘：Tauri 原生 tray
- **前端**：原生 HTML / CSS / JavaScript（无框架）
- **DSH 本体**：官方全局安装的 `@deepseek-ai/dsh`（不在启动器内打包）

## 四、核心功能

### 1. 异步 Harness 启动

- 点击“启动 DSH”后，在后台异步拉起 `dsh web --host 127.0.0.1 --port 3080`（官方默认端口，也可在设置中开启启动时自动拉起）。
- 启动过程不阻塞界面；后端轮询 `http://127.0.0.1:3080/` 直到出现 `__DSH_BOOT__` 标记。
- 端口已被已有 DSH 实例占用时，直接接管，不重复启动。
- 停止时用 `taskkill /T /F` 清理整个子进程树。

### 2. 系统托盘

- 右键菜单：显示控制台 / 打开 DSH 界面 / 复制访问地址 / 打开日志目录 / 打开会话目录 / 启动 / 停止 / 退出。
- 左键单击、双击托盘图标均可直接唤出主窗口。
- 点击窗口右上角关闭按钮时，隐藏到托盘而不是退出。
- 单实例：重复启动会聚焦已有实例，不会开多个。

### 3. 日志

- 后端实时捕获 DSH 的标准输出/错误。
- 前端通过事件流滚动显示，按级别着色（普通 / 警告 / 错误 / 就绪）。
- 同时写入 `%APPDATA%\dsh-launcher\logs\launcher.log`。
- 支持一键复制日志、导出日志到文件。

### 4. 会话管理

- 扫描 DSH_HOME（默认 `USERPROFILE\.dsh`，与官方 `dsh web` 共用同一套数据）`sessions` 下的 DSH 会话。
- 按工作区分组、支持按标题/工作区/会话内容做全文检索（返回匹配片段）。
- 点选后展示完整对话（用户 / 助手 / 工具调用 / 工具结果分色）。
- 支持将会话导出为 Markdown 文件。
- 删除会话时移动到 `sessions_trash` 备份目录；回收站支持逐条恢复、彻底删除、清空。

### 5. 看板娘背景

- 使用 `ui/kanban-background.png` 作为右下角背景角色。
- 可调节尺寸、透明度、左右位置，设置保存在 `localStorage`。

### 6. DSH 能力中心

- 插件管理：真实读取 profile bundles、`package.json` 依赖、`profiles/web/plugins`、`harness/plugins` 和 `.agent-presets`，展示真实存在的插件/预设。
  - 分类标签：内置（`@deepseek-ai` 本家）/ 扩展（市场下载）/ 自研（本地产出），内置不可卸载但可禁用，扩展可检查更新与卸载，自研可启停与卸载。
  - 批量操作：多选 / 全选，支持批量启用、批量禁用、批量卸载。
  - 扩展更新：对比 npm registry 最新版本，可一键更新。
  - **插件生效机制（重要）**：DSH 只加载「声明了 `dsh.bundle.patch` 且已进入 profile `dsh.profile.bundles` 层」的包。启动器在安装/注册/打开能力中心时会自动把这类依赖同步进 bundle 层（等效 DSH 官方 `dsh plugin` 的 reconcile）；未声明 `dsh.bundle` 的依赖只是普通库，不会被 DSH 加载，也不会被写入无效的 patch insert 条目。
  - 筛选排序：按分类/状态筛选，按默认/名称/版本/作者/分类排序，支持升降序切换。
  - 内置禁用写入 `disable` 条目后会自动用 `dsh --profile web --dump-config` 校验，失败自动回滚。
- 插件市场：真实读取 `awesome-dsh-plugin.com` 的社区插件列表，借鉴主流插件市场（VS Code Marketplace / Chrome Web Store）重设计——发现页（精选轮播 + 热门 / 最新上架 / 为你推荐 / 已装同类 / 最近浏览货架）、分类侧栏带计数、搜索高亮、综合/星标/最近添加/名称排序、npm/GitHub 来源与安装状态筛选、已安装/可更新视图、插件详情抽屉（完整信息 + 相关推荐 + 卸载/主页）、安装状态机（安装中/已安装/可更新）、本地缓存离线可用。安装调用官方 `dsh plugin --profile web add ...`。
- 技能管理：扫描各目录下的 `SKILL.md` 及其 frontmatter，展示真实技能与说明。
- MCP 服务：当前未发现 DSH 原生 MCP 配置来源，显示为空并如实标注“待接入”。

### 7. 多端管理

- 本地 Windows 端真实可用。
- 自动扫描（「多端管理」Tab →「自动扫描」）：枚举 Windows（PATH / npm 全局 / npx 缓存）与 WSL 各发行版（PATH + 常见非 PATH 位置，如 `~/node/bin/dsh`）中**所有**已安装的 dsh，逐个添加为端。
- WSL 端可真实启动/停止（`wsl -d <distro> -- dsh web` + `pkill` 清理）。
- 端连通检测真实执行（Windows 端口 / WSL 发行版可达）。
- SSH 远程端暂未实现，保持占位。

### 8. 运行与用量 / 配置

- `settings.yaml` 真实读写：内置 YAML 编辑器，直接编辑 DSH 配置，保存后重启 DSH 生效。
- `cordis.patch.yml` 真实读写：内置插件注册层编辑器，便于手动调整插件的 insert/disable。
- 配置导入导出：将启动器偏好（看板娘/多端/抽屉宽度）与 `settings.yaml` 打包为 JSON 保存/恢复。
- 环境信息面板：集中展示 DSH 路径/版本、DSH_HOME、端口、日志/会话目录、启动器路径，支持复制与打开目录。
- 界面缩放：全局缩放滑块（80%–130%）、锁定开关、恢复默认 100%，重启保持。
- 启动器偏好：启动行为、自动开浏览器、关闭行为、窗口置顶、开机自启、日志保留等均可配置并持久化。
- 用量统计、资源限额：DSH 未确认提供对应能力，保持“待接入”。

## 五、目录结构

```text
src-tauri/                 Rust 后端与 Tauri 配置
  src/
    main.rs                入口（windows_subsystem）
    lib.rs                 状态管理、58 个 Tauri 命令注册、托盘、窗口事件、开机自启
    harness.rs             dsh 子进程生命周期、日志捕获与轮转、就绪检测、WSL 启动
    sessions.rs            会话扫描 / 全文搜索 / 渲染 / 删除 / 回收站（zstd 解码）
    capabilities.rs        插件/技能/MCP 管理、WSL 扫描、插件市场安装、启动自检回滚
    prefs.rs               启动器偏好持久化、原子写、日志目录
  capabilities/default.json  前端权限（core + dialog）
  tauri.conf.json          应用与打包配置（productName、CSP、bundle）
  icons/icon.ico           应用与托盘图标
  Cargo.toml / Cargo.lock  Rust 依赖与锁定版本
  build.rs                 Tauri 构建脚本

ui/                        前端静态资源（原生 HTML/CSS/JS，无框架）
  index.html               页面结构与标签骨架
  core.js                  基础层：invoke 封装、工具、事件流、启动编排
  endpoints.js             多端管理：枚举/切换/增删/连通检测
  console.js               控制台：启停、状态、日志操作
  sessions.js              会话列表/检索/详情/回收站/导出
  capabilities.js          插件/技能/MCP 与插件市场（~52KB）
  settings.js              设置抽屉：通用/外观/运行配置/数据与维护/关于
  style.css                样式表（浅蓝白主题、弹性自适应布局）
  kanban-background.png    看板娘背景素材

build/                     图标与品牌素材（app-icon、logo、原始图）
docs/                      设计文档、评审记录、路线图
scripts/                   构建辅助脚本（图标生成、预设安装、品牌资源安装）
patches/                   patch-package 补丁（DSH 依赖 UI 修复）
agent-presets/             内置 Agent 预设示例（review / readonly-review）
.dsh/genui/                UI 设计稿与能力键（项目资产，非运行时数据）
```

## 六、环境要求

- Windows 10 / 11，已安装 **Microsoft Edge WebView2 Runtime**。
- Rust 稳定版（`x86_64-pc-windows-msvc`）。
- **Visual Studio 2022 Build Tools**，勾选“使用 C++ 的桌面开发”（提供 MSVC 链接器与 Windows SDK）。
- Node.js 20+，并用官方命令安装 DSH：

```powershell
npm install -g @deepseek-ai/dsh
dsh --version
```

## 七、构建与运行

构建前需要先进入 MSVC 环境（因为 Build Tools 的 `link.exe` 不在系统 PATH 中）：

```powershell
cmd /c ""C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvars64.bat" && cd /d D:\RJian\AIProjects\Projects\dsh-desktop\src-tauri && cargo build --release"
```

产物：

```text
src-tauri\target\release\dsh-launcher.exe
```

直接双击该 exe 即可运行；也可以为它创建桌面快捷方式。

如需生成 NSIS 安装包，可先安装 Tauri CLI 后执行：

```powershell
cargo install tauri-cli --locked
cd src-tauri
cargo tauri build
```

## 八、数据目录

启动器沿用 DSH 的标准数据目录（与官方 `dsh web` 完全一致）：

```text
USERPROFILE\.dsh\        （默认 DSH_HOME，可用 DSH_HOME 环境变量覆盖）
  profiles\     DSH profile 运行时与插件树
  sessions\     会话记录
  storages\     存储数据
  settings.yaml 设置
```

日志目录：

```text
%APPDATA%\dsh-launcher\logs\launcher.log
```

卸载、重装或删除启动器目录都不会影响会话记忆、设置与登录凭据。

## 九、关键设计决策

- **不打包 Node/DSH**：DSH 由用户通过官方命令全局安装，启动器只负责调用，保持 exe 轻量。
- **异步优先**：任何可能阻塞的 I/O（启动、就绪探测、日志读取）都不占用 UI 线程。
- **Rust 原生会话解析**：避免额外子进程，单二进制完成 zstd 解压和 JSON 解析。
- **删除可恢复**：会话删除采用“移动到备份目录”而非物理删除。
- **端口接管**：检测到已有 DSH 实例时直接复用，避免端口冲突。

## 十、已知说明

- 本项目为个人使用的本地工具，不提供跨平台安装包。
- 界面以豆包生成的 v0.4 设计稿为基础，浅蓝白主题、无 emoji、原生 JS 无框架依赖。
- 插件市场、插件/技能列表与操作已接入真实数据源与官方命令；MCP 配置、用量统计等 DSH 尚未暴露对应入口的能力，仍以“待接入”如实标注，不展示或伪造假数据。
- 模板内置 bundle（`@deepseek-ai/dsh-base`、`@deepseek-ai/dsh-web-app`，source=bundle）跟随 DSH 发行版提供，界面不提供无效的启停按钮；如需禁用内置组件，请在 `cordis.patch.yml` 中按其展开后的行 id 手动写 `disable` 条目。
- 界面已启用 CSP：脚本仅允许本地 `script-src 'self'`，网络访问限定为 npm registry 与插件市场域名；样式保留内联（`style-src 'unsafe-inline'`），图片限定本地 / GitHub 头像 / data URL。

## 十一、v1.0 更新记录

- 能力中心真实化：插件市场（实时数据、本地缓存离线可用、发现货架与精选轮播、个性化推荐、详情抽屉、安装状态机、搜索高亮）、技能列表、插件/预设真实读取与操作。
- 插件分类与批量管理：内置 / 扩展 / 自研标签，批量启用/禁用/卸载，扩展检查更新，内置安全禁用（自动校验回滚）。
- 会话能力：全文检索、导出 Markdown、回收站（恢复/彻底删除/清空）、点击高亮修复。
- 界面体验：全局缩放（80%–130% 可锁定）、滚动条滑块可见、插件市场响应式网格自适应。
- 工具能力：一键复制地址/日志/版本、日志导出、环境信息面板、cordis.patch.yml 编辑器、托盘菜单增强、窗口置顶。
- 安全加固：修复 DOM XSS、路径穿越、任意文件读写、命令注入；配置原子写入；`read_text_file` 改为选择器读文件。
- 多端：WSL 启停与连通检测；SSH 远程端明确标注“待开发”；WSL 探测过滤 Windows 互操作路径。

## 十二、v1.0.1 修复记录（审查整改）

- 插件安装真实生效：注册机制对齐 DSH 官方 bundle 层（`dsh.profile.bundles` + 包内 `dsh.bundle.patch`），废弃对 npm 插件写 `cordis.patch.yml` insert 的无效做法（该写法对 `@` 包名还是非法 YAML）；安装/打开能力中心自动 reconcile，历史“装了不生效”的插件自动修复。
- 启动行为：接管已有实例时也遵守“自动打开浏览器”；开机自启使用当前激活端（多端列表同步到后端）；端口占用策略文案改为“占用时报错”并在启动失败时给出引导。
- 数据与维护：导出范围三个勾选框真实生效；导入说明如实化；三个重置按钮全部真实实现（外观偏好 / DSH 配置文件 / 全部）。
- 可靠性：停止 DSH 先温和终止再强杀（避免打断会话写入）；stop 与就绪轮询竞态修复（generation）；接管外部实例时“停止”不再假装成功；日志超过 5MB 自动轮转；剪贴板复制中文不再乱码（UTF-16LE+BOM）；自启注册表值加引号；环境信息“安装目录”可打开；文件选择器应用扩展名过滤；本地插件导入改为异步。
- 会话：搜索片段保留原始大小写并做字符边界保护；搜索达到数量上限后仍按时间排序；新增 10 个单元测试（ID 校验、片段、标题解析、渲染过滤）。
- 界面与安全：脚本/样式外部化并启用 CSP（`script-src 'self'`）；清除所有内联事件（onerror）；切端后能力中心与技能筛选自动刷新；窗口位置支持负坐标（副屏在左侧）；默认工作区统一为 DSH_HOME；清理 `set_workspace` 死代码与 `LauncherPrefs` 的 kanban/drawer_width 死字段。

## 十三、插件市场 v2 更新记录（借鉴大众设计重设计）

- 布局：市场弹窗加宽为双栏（左分类侧栏 + 右主区），改为“发现页 + 网格列表 + 详情抽屉”三段式，对齐 VS Code Marketplace / Chrome Web Store 的主流信息架构。
- 发现机制：新增精选轮播（自动轮播 6s，支持箭头/圆点/悬停暂停）、货架式分组（热门插件 / 最新上架 / 为你推荐 / 已装插件的同类 / 最近浏览，横向滚动）。
- 推荐机制：个性化推荐——根据已装插件的分类构建兴趣画像，“为你推荐”按 兴趣匹配 + 分类相关度 + 热度 + 时效 加权；详情页展示同分类“相关推荐”。
- 数据机制：市场数据写入 localStorage 缓存（10 分钟 TTL），打开市场先渲染缓存秒开、后台静默刷新；离线时回退缓存并标注“离线 · 使用缓存”；头部显示数据同步时间与统计（总数 / 已安装 / 可更新）。
- 状态机制：安装状态机（未安装 → 安装中（防重复）→ 已安装 / 安装失败恢复；市场带 version 时对比本地版本显示“可更新”），安装/卸载后自动刷新本地插件列表与视图；详情抽屉内可直接安装 / 更新 / 卸载 / 打开主页。
- 搜索与浏览：搜索支持名称 / 作者 / 说明 / npm 包名并高亮命中词；最近浏览持久化（最多 12 条）；Esc / 点击遮罩关闭；分类筛选与视图切换持久化到 localStorage。
- 移除：原“列数滑杆”与“推荐开关”改为响应式自动网格与「发现 / 全部 / 已安装 / 可更新」视图 Tab。
