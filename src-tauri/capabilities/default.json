{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "DSH Launcher 主窗口默认权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
